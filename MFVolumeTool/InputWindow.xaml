<Window x:Class="MFVolumeTool.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="InputWindow" Height="175.924" Width="381.545" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Loaded="Window_Loaded">
    <Grid>
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="BtnConfirm" Content="确认" Grid.Column="1" Margin="10,23.051,10,0" VerticalAlignment="Top" Click="BtnConfirm_Click" IsDefault="True"/>
            <Button x:Name="BtnCancal" Content="取消" Grid.Column="1" Margin="9.6,61,10.2,0" Click="BtnCancal_Click" VerticalAlignment="Top" IsCancel="True"/>
            <TextBox Grid.Column="0" x:Name="TbSerivce" Height="23" Margin="10,0,10,28.9" TextWrapping="Wrap" VerticalAlignment="Bottom"/>
            <Label Grid.Column="0" Content="请输入服务信息" HorizontalAlignment="Left" Margin="10,25.061,0,0" d:LayoutOverrides="HorizontalAlignment" VerticalAlignment="Top"/>
        </Grid>
    </Grid>
</Window>
